version: 2

models: 
  - name: mart_driver_race_pace
    description: "Mart table for driver pace analysis per race, green flag laps only"
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - year
              - round_number
              - driver